{
  "name": "bgo-whatsapp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone WhatsApp (Meta Cloud API) notification backend for BGO Shine Hub — keeps the access token off the browser, and gives Meta a webhook to call.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
