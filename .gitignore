#env
.env
.env.*

# Claude Code local session/config state — not meant to be shared
.claude/

# Standalone M-Pesa backend — kept out of this repo (has its own .env with
# real payment-provider secrets once configured; see its own README locally)
mpesa-server/

# Standalone WhatsApp backend — same reasoning as mpesa-server/ above
whatsapp-server/

# Standalone user-admin backend — holds a Firebase service account key once
# configured (see its own README locally); same reasoning as mpesa-server/
user-admin-server/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env