#env
.env
.env.*

# Firebase service account keys downloaded as JSON (real credentials) -
# should live in a server's .env instead, but excluded here too in case one
# ends up dropped straight into a backend folder
*firebase-adminsdk*.json

# Demo/QA seed dataset for app-data-server - not needed to run the app for
# real, and shouldn't ship in the repo
app-data-server/seed-data.json

# Claude Code local session/config state - not meant to be shared
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env