{
  "name": "bgo-app-data-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone backend for BGO Shine Hub that stores every business record (washes, payments, staff, services, inventory, etc.) in a real shared MySQL database on cPanel, replacing the per-browser IndexedDB store so every device sees the same data.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node seed-cli.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0",
    "mysql2": "^3.11.3"
  }
}
